import {LitElement, html, customElement} from 'lit-element';

@customElement('side-menu')
export class SideMenu extends LitElement {

    // Define the element's template
    render() {
        return html`<div>SideMenu</div>`;
    }
}
